import XCTest
@testable import QRScannerViewKit

final class QRScannerViewKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(QRScannerViewKit().text, "Hello, World!")
    }
}
